spring:
  application:
    name: dubbo-common

server:
  port: 8021

dubbo:
  application:
    id: dubbo-common # 当前dubbo应用id
    name: dubbo-common # 当前dubbo应用名称
  registry:
    address: zookeeper://127.0.0.1:2181 # 注册中心
    timeout: 60000
    group: dubbo
    check: false